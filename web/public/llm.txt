# mailrs - AI Email API

> Full-featured email server API: send, read, search, reply, manage accounts/domains/aliases/groups/apps — via MCP or REST.

Base URL: https://mail.golia.ai
Auth: Bearer API key or session token on all requests
Full API docs: https://mail.golia.ai/llm-full.txt
Interactive docs: https://mail.golia.ai/docs.html
OpenAPI spec: https://mail.golia.ai/openapi.json

## MCP (recommended)

Streamable HTTP endpoint: https://mail.golia.ai/mcp
82 tools auto-discovered, grouped by category (identical on both server lanes):

```json
{"mcpServers":{"mailrs":{"type":"streamable-http","url":"https://mail.golia.ai/mcp","headers":{"Authorization":"Bearer <API_KEY>"}}}}
```

**Email:** send_email, read_thread, search_conversations, reply_email, list_conversations, list_mailboxes, get_categories, search_contacts, send_scheduled_email, get_thread_summary, get_inbox_metrics
**Threads:** mark_thread_read, mark_thread_unread, mark_all_read, star_thread, unstar_thread, pin_thread, unpin_thread, snooze_thread, unsnooze_thread, archive_thread, unarchive_thread, delete_thread
**Drafts & Templates:** list_drafts, save_draft, delete_draft, list_templates, save_template, delete_template, list_signatures, save_signature, delete_signature
**Accounts:** list_accounts, create_account, remove_account, set_account_password
**Permissions:** list_groups, get_account_permissions, get_my_permissions, add_account_to_group, remove_account_from_group
**Domains:** list_domains, add_domain, remove_domain
**Aliases:** list_aliases_admin, add_alias, remove_alias
**Email Groups:** list_email_groups, create_email_group, delete_email_group, get_email_group_members, add_email_group_member, remove_email_group_member
**Apps & Webhooks:** list_apps, create_app, delete_app, list_webhooks, create_webhook, delete_webhook
**Queue & Scheduling:** get_queue, list_admin_queue, retry_queue_message, list_failed_outbound, list_own_scheduled, list_scheduled_outbound, cancel_scheduled, reschedule_scheduled
**Encryption:** list_own_encryption_keys, get_public_key_of, set_encryption_key, delete_encryption_key
**Greylist:** list_greylist_local, greylist_local_add, greylist_local_remove
**Audit & System:** get_audit_log, audit_list_conversations, audit_read_thread, get_system_config, set_system_config, reset_system_config, get_server_info, reconcile_maildir

## REST API

### Email
- POST /api/mail/send — send email (JSON)
- POST /api/mail/send-multipart — send with attachments
- POST /api/mail/unsubscribe — RFC 8058 one-click leave; body names the message, the server posts to the URL that message carried
- GET /api/conversations — list conversations (?q=keyword, ?category=, ?limit=)
- GET /api/conversations/{thread_id} — thread messages
- GET /api/mail/messages/{uid} — read message; a meeting invitation comes back as `invite_method` + `invite_payload`
- POST /api/invites/{uid}/rsvp — answer an invitation (ACCEPTED / TENTATIVE / DECLINED); records the choice and queues an iTIP REPLY to the organiser
- POST /api/invites/{uid}/counter — propose a different time
- GET /api/calendar/conflicts?start=&end= — what else is already in the calendar over that window
- POST /api/conversations/{thread_id}/read — mark read
- POST /api/conversations/{thread_id}/star — star
- POST /api/conversations/{thread_id}/archive — archive
- DELETE /api/conversations/{thread_id} — delete thread
- GET /api/mail/folders — list folders
- GET /api/contacts — search contacts

### Connected mailboxes
Gmail, Outlook, QQ or any IMAP/POP3/JMAP server, read into the same
lists as this one and sent through the provider's own server.
- GET /api/accounts/external — the connected mailboxes and the state of each; no secret is ever returned
- POST /api/accounts/external — connect one; servers discovered from the address unless `incoming`/`outgoing` are given; the credential is proved before it is stored
- DELETE /api/accounts/external/{id} — disconnect; removes the sealed credential and the sync markers, keeps the mail
- POST /api/accounts/external/{id}/paused — `{"paused": true|false}`; stops the reading, never the sending
- GET /api/accounts/external/settings?email= — what to fill in, before anything is saved
- GET /api/accounts/external/oauth/{provider} — begin an OAuth connection (Gmail and Outlook refuse passwords for mail clients)

### Admin (requires permissions)
- GET/POST/DELETE /api/admin/domains — domain CRUD
- GET/POST/DELETE /api/admin/accounts — account CRUD
- GET/POST/DELETE /api/admin/aliases — alias CRUD
- GET/POST/DELETE /api/admin/email-groups — email group CRUD + /members
- GET/POST/DELETE /api/admin/groups — group CRUD + /permissions + /members
- GET/POST/DELETE /api/admin/apps — app CRUD + /scopes
- GET /api/admin/permissions — list all permissions
- GET /api/admin/dmarc/reports — inbound DMARC aggregate reports, newest first (?limit=) (admin.domains)
- GET /api/admin/dmarc/reports/{sid} — one report with its per-source rows (admin.domains)
- GET /api/admin/dmarc/sources — per-source-IP rollup with alignment rate (?domain=, ?days=) (admin.domains)
- GET/POST /api/admin/greylist/local-lists, DELETE /api/admin/greylist/local-lists/{id} — local greylist white/black CRUD (admin.greylist)
- POST /api/admin/reconcile-maildir — index maildir files missing from PG; {"dry_run": true} reports only (internal.rpc)

### Agent
- POST/GET/DELETE /api/agent/keys — API key management
- POST/GET/DELETE /api/agent/webhooks — webhook subscriptions

### Status
- GET /api/status, /api/health, /api/readiness

## Contact

Admin: lihao@golia.jp
