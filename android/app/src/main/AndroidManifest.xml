<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Mail is the whole app; without this it can do nothing. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      "You have mail". Asked for at the moment it means something —
      after signing in — rather than at first launch, which is a prompt
      about nothing. Refusing it costs only the notification: the app
      works the same and the check simply says nothing.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mailrs">
        <!-- allowBackup=false on purpose: the session token lives in this
             app's private storage, and cloud backup would copy it off the
             device. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Mailrs.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              A mail client that cannot be shared to is a mail client
              somebody leaves. These are the three ways Android asks one
              to write a message: a mailto: link tapped anywhere, and
              the share sheet with text or a file.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="mailto" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- Long press on the launcher icon. -->
            <intent-filter>
                <action android:name="jp.golia.mailrs.COMPOSE" />
                <action android:name="jp.golia.mailrs.SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <!--
          Attachments leave through a content:// URI with a per-open
          grant. A file:// path to another app has thrown since API 24,
          and making the copy world-readable instead would be a worse
          way to make it work.
        -->
        <!-- Archive, from the notification shade. Not exported: only
             this app's own PendingIntent triggers it. -->
        <receiver
            android:name=".wire.ArchiveFromNotification"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
