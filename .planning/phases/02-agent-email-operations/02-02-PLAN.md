---
phase: 02-agent-email-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/server/src/web/conversations.rs
  - crates/server/src/web/mail.rs
autonomous: true
requirements: [MAIL-04, MAIL-05]

must_haves:
  truths:
    - "Agent can read full message content (text_body, html_body, attachments) via GET /api/conversations/{thread_id}"
    - "Agent can list conversations with pagination and folder/category filters"
    - "Agent can search messages by text query via /api/conversations/search"
  artifacts:
    - path: "crates/server/src/web/conversations.rs"
      provides: "Conversation list, search, and thread detail endpoints"
    - path: "crates/server/src/web/mail.rs"
      provides: "Message detail endpoint"
  key_links:
    - from: "API key Bearer auth"
      to: "conversation/mail endpoints"
      via: "AuthUser extractor (Phase 1)"
      pattern: "AuthUser"
---

<objective>
Verify and test that existing read/list/search endpoints work correctly with API key authentication for agent use cases.

Purpose: MAIL-04 and MAIL-05 are already implemented — the endpoints exist and AuthUser extractor from Phase 1 makes them API-key-accessible. This plan validates they work as expected and adds integration test coverage.
Output: Integration tests confirming agent read/search workflows.
</objective>

<execution_context>
@/Users/doracawl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/doracawl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-agent-email-operations/02-RESEARCH.md

<interfaces>
<!-- Existing endpoints (already working, need verification) -->
GET /api/conversations?limit=20&category=personal&folder=INBOX
GET /api/conversations/search?q=invoice&limit=10
GET /api/conversations/{thread_id}  → returns all messages with full body
GET /api/mail/messages/{uid}        → returns MessageDetail with text_body, html_body, attachments
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration tests for agent read/list/search operations</name>
  <files>crates/server/src/web/mail.rs, crates/server/src/web/conversations.rs</files>
  <behavior>
    - Test: GET /api/conversations returns list with pagination (limit, before params)
    - Test: GET /api/conversations/search?q=keyword returns matching conversations
    - Test: GET /api/conversations/{thread_id} returns full message bodies (text_body, html_body present)
    - Test: All above endpoints accept API key auth (Bearer mlrs_...) — verified via AuthUser extraction
    - Test: Superadmin key with domain access can read other users' conversations
  </behavior>
  <action>
  Write integration tests that verify the read/search endpoints respond correctly. Since these are existing endpoints, tests should:

  1. **Test response structure:** Verify that conversation list returns expected JSON shape (id, subject, participants, snippet, etc.)
  2. **Test search:** Verify search endpoint accepts query parameter and returns filtered results
  3. **Test thread detail:** Verify GET /api/conversations/{thread_id} returns messages with text_body and html_body fields
  4. **Test auth:** Verify that requests without auth return 401, and requests with valid API key auth succeed (use the verify_api_key_logic test pattern from Phase 1 — test at the AuthUser extractor level rather than requiring a running server)

  Place tests in the appropriate `#[cfg(test)]` module. If existing tests already cover some scenarios, extend rather than duplicate. Focus on testing the response structure that agents will consume.

  NOTE: These endpoints already work. The goal is test coverage for agent use cases, not fixing bugs. If any endpoint has issues for agent scenarios (e.g., missing fields in response), document them as findings for a gap closure plan.
  </action>
  <verify>
    <automated>cd /Users/doracawl/workspace/goliajp/mailrs && cargo test -p mailrs-server -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <done>
  - Integration tests exist for conversation list, search, and thread detail endpoints
  - Tests verify response structure suitable for agent consumption
  - All tests pass
  </done>
</task>

</tasks>

<verification>
- `cargo test -p mailrs-server` passes with new integration tests
- Test coverage confirms agents can read messages, list conversations, and search
</verification>

<success_criteria>
- Agent can authenticate with API key and read full message content
- Agent can list and search conversations with proper filtering
- Test coverage validates these flows
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-email-operations/02-02-SUMMARY.md`
</output>
