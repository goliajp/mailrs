---
phase: 04-mcp-server
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - crates/server/src/web/mod.rs
  - crates/server/src/main.rs
autonomous: false
requirements: [MCP-02]

must_haves:
  truths:
    - "GET/POST /mcp 端点响应 MCP protocol 初始化请求"
    - "未认证请求 /mcp 返回 401"
    - "Claude Code 配置后可通过 MCP 调用邮件工具"
  artifacts:
    - path: "crates/server/src/web/mod.rs"
      provides: "MCP service mounted at /mcp in router"
      contains: "setup_mcp"
  key_links:
    - from: "crates/server/src/web/mod.rs"
      to: "crates/server/src/mcp/mod.rs"
      via: "calls setup_mcp() and merges into app router"
      pattern: "mcp::setup_mcp"
    - from: "crates/server/src/mcp/auth.rs"
      to: "crates/server/src/web/mod.rs router"
      via: "auth middleware layer applied to MCP routes"
      pattern: "mcp_auth_middleware"
---

<objective>
Wire MCP service into the main Axum router at `/mcp` and verify end-to-end with Claude Code.

Purpose: Makes the MCP server accessible over HTTP, completing the Streamable HTTP transport requirement.
Output: Live `/mcp` endpoint with auth, verified by Claude Code.
</objective>

<execution_context>
@/Users/doracawl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/doracawl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-mcp-server/04-RESEARCH.md
@.planning/phases/04-mcp-server/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From crates/server/src/mcp/mod.rs:
```rust
pub fn setup_mcp(state: Arc<WebState>) -> axum::Router;
```

From crates/server/src/mcp/auth.rs:
```rust
pub(crate) async fn mcp_auth_middleware(...) -> ...;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount MCP service in Axum router and release</name>
  <files>crates/server/src/web/mod.rs, crates/server/src/main.rs</files>
  <action>
1. In `crates/server/src/web/mod.rs` `router()` function:
   - Import `crate::mcp`
   - After the main `app` is built (before `with_state`), merge the MCP router:
     ```rust
     let mcp_router = mcp::setup_mcp(state.clone());
     app = app.merge(mcp_router);
     ```
   - The MCP router from `setup_mcp()` should already include the auth middleware layer (applied in Plan 01's setup_mcp function). If not, apply it here:
     ```rust
     let mcp_router = mcp::setup_mcp(state.clone())
         .layer(middleware::from_fn_with_state(state.clone(), mcp::auth::mcp_auth_middleware));
     ```
   - IMPORTANT: The MCP route should NOT be behind the general rate limiter — MCP sessions are long-lived. Place the merge BEFORE the rate limit layer is applied, or ensure the MCP router is merged after `with_state` using axum's routing properly. The cleanest approach: build the MCP router separately with its own auth layer but without the general rate limiter, then merge.

2. Verify the server starts and `/mcp` is reachable:
   - `cargo build -p mailrs-server` must pass
   - Run full test suite: `cargo test --workspace`

3. Run `./scripts/release.sh` to deploy to production.
  </action>
  <verify>
    <automated>cd /Users/doracawl/workspace/goliajp/mailrs && cargo test --workspace 2>&1 | tail -10</automated>
  </verify>
  <done>MCP service mounted at /mcp in the main router. Server compiles and all tests pass. Released to production.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify MCP tools work end-to-end with Claude Code</name>
  <action>
User verifies that MCP server works with Claude Code by configuring it and testing all 5 tools.

What was built: MCP server embedded in mailrs at /mcp with Streamable HTTP transport, 5 email tools (send, read, search, reply, list_conversations), and API key auth.

How to verify:
1. Configure Claude Code MCP server:
   ```bash
   claude mcp add --transport http mailrs https://mail.golia.jp/mcp \
     --header "Authorization: Bearer mlrs_xxxxxxxx_yyyyyyyyyyyyyyyyyyyy"
   ```
   (Use a valid API key created via the admin UI or API)

2. In a Claude Code session, test each tool:
   - Ask Claude to "list my recent emails" — should call list_conversations
   - Ask Claude to "search for emails about [topic]" — should call search_emails
   - Ask Claude to "read email with uid [N]" — should call read_email
   - Ask Claude to "send an email to test@example.com with subject Test" — should call send_email
   - Ask Claude to "reply to thread [id] saying thanks" — should call reply_email

3. Verify auth: try with an invalid API key — should get auth error

4. Verify tool descriptions appear in Claude Code's tool list
  </action>
  <verify>User confirms all 5 tools work via Claude Code</verify>
  <done>Claude Code can discover and successfully invoke all 5 MCP tools with proper authentication.</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` passes
- Server starts without errors
- `/mcp` endpoint responds to MCP initialize request
- Unauthenticated requests to `/mcp` return 401
- Claude Code can discover and call all 5 tools
</verification>

<success_criteria>
- MCP server live at https://mail.golia.jp/mcp
- Claude Code successfully calls at least one MCP tool (e.g., list_conversations)
- Auth enforced (invalid key returns 401)
</success_criteria>

<output>
After completion, create `.planning/phases/04-mcp-server/04-02-SUMMARY.md`
</output>
