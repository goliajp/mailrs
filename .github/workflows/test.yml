name: test

# Run cargo test + bun test on every push to a git-flow branch. We don't
# use PRs (see .claude/rules/common/git-workflow.md) — branch start/finish
# is the merge path, so push triggers cover every code change.
#
# develop/master: integration + production tips
# feature/**:   in-progress feature work (off develop)
# bugfix/**:    in-progress bug fixes (off develop)
# release/**:   release candidates (off develop, into master)
# hotfix/**:    urgent prod fixes (off master, into master + develop)

on:
  push:
    branches:
      - develop
      - master
      - 'feature/**'
      - 'bugfix/**'
      - 'release/**'
      - 'hotfix/**'
      - 'arch/**'

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  # Pin a single workspace target dir inside $GITHUB_WORKSPACE so the
  # actions/cache@v4 step below can prime it across runs.
  CARGO_TARGET_DIR: ${{ github.workspace }}/target

jobs:
  rust:
    name: cargo test --workspace
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: install rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: cache cargo registry + target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            cargo-${{ runner.os }}-

      - name: cargo fmt --check
        run: cargo fmt --all -- --check

      - name: cargo clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: pre-pull testcontainer images (retry against Docker Hub flake)
        # Integration tests in mailbox / outbound-queue / mail-builder spin
        # up real `pgvector/pgvector:pg18` + `axllent/mailpit:latest`
        # containers via testcontainers. Docker Hub registry sporadically
        # times out pulls (e.g. 2026-06-03 04:27 UTC outage killed the
        # b6335f6 run). Pre-pull with retry so the test step finds the
        # images already local and never hits the Hub.
        run: |
          set -e
          for img in pgvector/pgvector:pg18 axllent/mailpit:latest; do
            ok=0
            for i in 1 2 3 4 5; do
              if docker pull "$img"; then ok=1; break; fi
              echo "::warning::docker pull $img attempt $i failed, retrying in 15s"
              sleep 15
            done
            [ "$ok" = 1 ] || { echo "::error::docker pull $img failed after 5 attempts"; exit 1; }
          done

      - name: cargo test --workspace (skip perf_gates — CI runners are too noisy)
        # Each *_under_budget test asserts wall-clock under a fixed
        # budget tuned for M-series Mac at idle. GH Actions ubuntu
        # runners are ~3× slower and have variable CPU. Skip the gates
        # here; they run locally before `scripts/release.sh` ships and
        # via `cargo bench` for trend tracking.
        run: cargo test --workspace --no-fail-fast -- --skip _under_budget

      - name: cargo test --workspace (spg embed axis)
        # Same suite against the in-process spg-embedded backend —
        # the production shape after the SPG cutover. No containers:
        # every fixture runs on SpgPool::connect_in_memory(), so this
        # axis is fast and docker-independent. The PG axis above stays
        # as the reference-backend gate for the published stones.
        run: >
          cargo test --workspace --no-fail-fast
          --features mailrs-server/spg,mailrs-mailbox/spg,mailrs-outbound-queue/spg,mailrs-shield/spg,mailrs-dmarc/spg
          -- --skip _under_budget

      - name: cargo build (4-process binaries — split topology gate)
        # Phase 2/3/4 of the split-fastcore plan. Builds every binary
        # the deploy compose mounts so a CI green means the staging
        # image has all four roles.
        run: |
          cargo build --release -p mailrs-server --features spg,render-preview,core-rpc
          cargo build --release -p mailrs-receiver
          cargo build --release -p mailrs-webapi
          cargo build --release -p mailrs-sender
          cargo build --release -p mailrs-mailbox-kevy  # Phase 7 fastcore
          cargo build --release -p mailrs-fastcore       # Phase 8 fastcore binary
          cargo build --release -p mailrs-fastcore --bin mailrs-fastcore-migrate  # Phase 10 import bin

      - name: cargo check (default mailrs-server — ironrule guard)
        # Asserts the prod-default build still produces the same artifact
        # as develop. Phase 2 `core-rpc` is feature-flagged off here, so
        # any unexpected pull of mailrs-core-api into the default graph
        # would be caught.
        run: cargo check -p mailrs-server

  web:
    name: web check + test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4

      - name: install bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: bun install
        run: bun install --frozen-lockfile

      - name: bun run check (tsc + eslint + prettier)
        run: bun run check

      - name: bun run test:coverage
        run: bun run test:coverage
